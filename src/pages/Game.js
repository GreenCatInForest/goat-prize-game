import { NavBar } from "../components/NavBar";
import { GameComponent } from "../components/GameComponent";

export const Game = () => {
  return (
    <div>
      <NavBar />
      <GameComponent />
    </div>
  );
};
